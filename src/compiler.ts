import type { Abi } from "abitype";
import type { Program } from "./types/ast";
import type { Hex } from "./types/utils";

export const compileAbi = (program: Program): Abi => {};

export const compileCode = (program: Program): Hex => {};
